apiVersion: v1
kind: Service
metadata:
  name: configserver
  labels:
    app: configserver
spec:
  type: ClusterIP
  selector:
    app: configserver
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
